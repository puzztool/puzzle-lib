{
  "name": "puzzle-lib",
  "version": "0.4.2",
  "description": "Library of puzzle-solving algorithms",
  "main": "./built/index.js",
  "types": "./built/index.d.ts",
  "files": [
    "built/"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "semistandard && tslint --project .",
    "lint-fix": "semistandard --fix && tslint --fix --project .",
    "prepare": "npm run build",
    "test": "npm run lint && npm run build && npm run test-only",
    "test-only": "mocha"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kfarnung/puzzle-lib.git"
  },
  "author": "Kyle Farnung",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kfarnung/puzzle-lib/issues"
  },
  "homepage": "https://github.com/kfarnung/puzzle-lib#readme",
  "devDependencies": {
    "mocha": "^5.2.0",
    "semistandard": "^12.0.1",
    "tslint": "^5.10.0",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "left-pad": "^1.3.0"
  }
}
